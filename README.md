How to import (tsconfig.json) file --- tsc init

After been created go to <!-- Module --> then to "rootdir" and change root dir "./" to "./src/

Then go to your file and create (src) dir in the parent dir